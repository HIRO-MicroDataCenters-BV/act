[project]
name = "act"
version = "0.1.0"
description = "Automated Configuration Testing for CAPE Pulumi programs"
requires-python = ">=3.11"
dependencies = [
    "pulumi>=3.230.0",
    "pytest>=9.0.3",
]

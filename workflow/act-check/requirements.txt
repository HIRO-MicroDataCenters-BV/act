# Source archives, not git URLs: a git install recurses into submodules, and the CAPE SDK
# tree carries a submodule pointer without a .gitmodules entry, which aborts the clone.
act[acv] @ https://github.com/HIRO-MicroDataCenters-BV/act/archive/refs/tags/0.9.4.tar.gz
pulumi-cape @ https://github.com/cape-project-eu/cape-sdks/archive/441196726912c3719d282f90623fa5aa3d176794.tar.gz#subdirectory=python
# Provider SDKs the bundled schemas correspond to; a program can only be loaded if its SDK is importable.
pulumi-kubernetes>=4.28.0
# The engine's Python action wrapper ships protobuf gencode 6.31.1; the runtime must not be older.
protobuf>=6.31.1
